<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.triplan.mapper.FlightMapper">

    <insert id="flightInsert">
        INSERT INTO flight(item_id, departure, destination, flight_type,
        number_of_person, seat_class, passenger_type, airline, baggage, seat_number)
        VALUES (#{itemId}, #{departure}, #{destination}, #{flightType},
        #{numberOfPerson}, #{seatClass}, #{passengerType}, #{airline}, #{baggage}, #{seatNumber})
    </insert>

    <select id="flightSelect" resultType="com.triplan.domain.FlightVO">
        select * FROM flight
        WHERE flight_id=#{flightId}
    </select>

    <update id="flightUpdate">
        UPDATE flight
        SET flight_id = #{flightId}
        item_id = #{itemId}
        departure = #{departure}
        destination = #{destination}
        flight_type = #{flightType}
        number_of_person = #{numberOfPerson}
        seat_class = #{seatClass}
        passenger_type =#{passengerType}
        airline = #{airline}
        baggage = #{baggage}
        seat_number = #{seatNumber}
    </update>

    <delete id="flightDelete">
        DELETE FROM flight
        WHERE flight_id=#{flightId}
    </delete>
</mapper>