<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.triplan.mapper.MemberMapper">

    <update id="updateBasicInfo">
        UPDATE member
        SET nickname = #{nickname},
            profile_img = #{profileImg},
            updated_at = CURRENT_TIMESTAMP
        WHERE member_id = ${memberId}
    </update>

    <update id="updateEmail">
        UPDATE member
        SET email = #{email},
            email_confirm = #{emailConfirm},
            updated_at = CURRENT_TIMESTAMP
        WHERE member_id = ${memberId}
    </update>

    <update id="updatePassword">
        UPDATE member
        SET password = #{password},
            updated_at = CURRENT_TIMESTAMP
        WHERE member_id = ${memberId}
    </update>

    <insert id="insert">
        INSERT INTO member(email,password,nickname,provider)
        values (#{email},#{password},#{nickname},#{provider})
    </insert>

    <select id="select" resultType="com.triplan.domain.MemberVO">
        SELECT * FROM member where member_id=${memberId}
    </select>

    <update id="update">
        update member
        set state=#{state},
            updated_at = CURRENT_TIMESTAMP,
            point=#{point},
            account=#{account},
            grade=#{grade}
        where member_id=#{memberId}
    </update>

    <delete id="delete">
        delete from member where member_id=#{memberId}
    </delete>
</mapper>

