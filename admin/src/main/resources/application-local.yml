server:
  port: 8082

spring:
  config:
    activate:
      on-profile: local

  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url:
    username:
    password:

  thymeleaf:
    cache: false

  devtools:
    livereload:
      enabled: true
    restart:
      enabled: true